apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-dxpgateway
spec:
  volumeMode: Block
  storageClassName: topolvm-provisioner
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-sdcard
spec:
  volumeMode: Block
  storageClassName: topolvm-provisioner
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
